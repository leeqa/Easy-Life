<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Main.BudgetManager.DetailBudget.ExpenseFragment.ExpenseFragment">

    <TextView
        android:id="@+id/expense_fragment_total"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentBottom="true"
        android:layout_marginLeft="80dp"
        android:padding="10dp"
        android:text="Current:-"
        android:textColor="#000"
        android:textSize="25dp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/expense_current_amount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginLeft="20dp"
        android:layout_toRightOf="@id/expense_fragment_total"
        android:padding="10dp"
        android:text="amount"
        android:textColor="#000"
        android:textSize="25dp"
        android:textStyle="bold" />

    <ListView
        android:id="@+id/expense_fragment_listView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/expense_current_amount"
        android:padding="10dp" />


    <ProgressBar
        android:id="@+id/expense_received_progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone" />

    <TextView
        android:id="@+id/expense_received_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="NOTHING TO SHOW"
        android:textSize="30dp"
        android:visibility="gone" />
</RelativeLayout>